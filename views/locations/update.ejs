<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Location</title>
</head>

<body>
    <h1>Edit Location</h1>
    <form action="/locations/<%=location._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="location[title]" value="<%=location.title %>">
        </div>
        <div>
            <label for="address">Address</label>
            <input type="text" id="address" name="location[address]" value="<%= location.address%>">
        </div>
        <button>Update Location</button>
    </form>
    <a href="/locations/<%= location._id%>">Back To Location</a>

</body>

</html>